apiVersion: v2
name: openvpn-admin
description: |
  OpenVPN server + ovpn-admin web UI in Kubernetes.
  PKI is stored in K8s Secrets (etcd) — no PVC required.
  Admin UI is protected by htpasswd authentication.
type: application
version: "1.0.0"
appVersion: "latest"
kubeVersion: ">=1.21.0-0"
keywords:
  - openvpn
  - vpn
  - ovpn-admin
sources:
  - https://github.com/VorkerJ/ovpn-admin
maintainers:
  - name: VorkerJ
    url: https://github.com/VorkerJ
